spring:
  datasource:
    # url: jdbc:postgresql://localhost:5432/dialdatadb
    url: jdbc:postgresql://dial-data.clg6kxm72irr.us-east-1.rds.amazonaws.com:5432/dialDB
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

dial:
  data:
    bitcoin:
      blockstore: postgres # Available options: memory, postgres

management:
  health:
    db:
      enabled: true
